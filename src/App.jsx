import {useEffect,useMemo,useState} from 'react'
import {Radio,Waves,Map,MessageCircle,UserRound,Sparkles,Search,Bell,Navigation,Users,Heart,Send,LogIn,LogOut,Plus,X,RefreshCw,Settings,Shield,SlidersHorizontal,Copyright,FileText,Video,Image as ImageIcon,EyeOff,ChevronRight,Flag,UserPlus,UserCheck,Clock3,Globe2,Play,MoreHorizontal,CircleDollarSign,Lock,CheckCircle2,Gamepad2,Bookmark,Share2} from 'lucide-react'
import {getCurrentUser,ensureUserDocument,getUserProfile,updateUserProfile,listMoments,listUsers,listNotifications,listUserMoments,createMoment,joinMoment,reactToMoment,addComment,listComments,followUser,unfollowUser,isFollowing,subscribeUser,unsubscribeUser,isSubscribed,listActivity,login,register,logout,uploadMedia,createReport} from './services/appwrite'
import {AdBanner320,AdBanner300} from './ads/Ads'

const tabs=[['now','NOW',Radio],['flow','FLOW',Waves],['scenes','SCENES',Map],['talk','TALK',MessageCircle],['space','SPACE',UserRound],['monetize','MONETIZE',CircleDollarSign]]
const categories=['do','play','people','event']
const defaultSettings={privateAccount:false,ghostMode:false,notifyJoins:true,notifyReactions:true,notifyComments:true,personalizedDiscovery:true,originalContent:true,allowMessages:true,allowMentions:true,showLocation:false,autoplayMedia:true,dataSaver:false}

export default function App(){
 const [tab,setTab]=useState('now'),[user,setUser]=useState(null),[moments,setMoments]=useState([]),[profiles,setProfiles]=useState({}),[loading,setLoading]=useState(true),[refreshing,setRefreshing]=useState(false),[authOpen,setAuthOpen]=useState(false),[createOpen,setCreateOpen]=useState(false),[searchOpen,setSearchOpen]=useState(false),[notificationsOpen,setNotificationsOpen]=useState(false),[settingsOpen,setSettingsOpen]=useState(false),[profileId,setProfileId]=useState(null),[notice,setNotice]=useState('')
 const load=async(silent=false)=>{if(!silent)setLoading(true);try{const current=await getCurrentUser();setUser(current);if(current){try{await ensureUserDocument(current)}catch(e){setNotice(e?.message||'Profile setup failed.')}}const ms=await listMoments();setMoments(ms);const ids=[...new Set(ms.map(m=>m.ownerId).filter(Boolean))],ps={};await Promise.all(ids.map(async id=>{try{ps[id]=await getUserProfile(id)}catch{}}));setProfiles(ps)}catch(e){setNotice(e?.message||'WORLD could not load live data.')}finally{if(!silent)setLoading(false)}}
 useEffect(()=>{load()},[])
 useEffect(()=>{const fn=e=>{if(!['INPUT','TEXTAREA','SELECT'].includes(e.target.tagName)){if(e.type==='contextmenu'||e.type==='copy'||e.type==='cut')e.preventDefault()}};document.addEventListener('contextmenu',fn);document.addEventListener('copy',fn);document.addEventListener('cut',fn);return()=>{document.removeEventListener('contextmenu',fn);document.removeEventListener('copy',fn);document.removeEventListener('cut',fn)}},[])
 const requireUser=()=>{if(!user){setAuthOpen(true);return false}return true}
 const refresh=async()=>{setRefreshing(true);try{await load();setNotice('WORLD refreshed.')}finally{setRefreshing(false)}}
 const join=async id=>{if(!requireUser())return;try{await joinMoment(id,user.$id);setNotice('You joined this Moment.');await load(true)}catch(e){setNotice(e?.message||'Join failed.')}}
 const react=async id=>{if(!requireUser())return;try{await reactToMoment(id,user.$id);setNotice('Reaction added.');await load(true)}catch(e){setNotice(e?.message||'Reaction failed.')}}
 const comment=async(id,text)=>{if(!requireUser())return;try{await addComment(id,user.$id,text);setNotice('Comment added.');await load(true)}catch(e){setNotice(e?.message||'Comment failed.')}}
 return <div className="app"><header className="topbar"><button className="brand" onClick={()=>setTab('now')}><span className="brand-mark"><span/></span><b>WORLD</b></button><div className="top-actions"><button className="icon-btn" onClick={refresh} title="Refresh"><RefreshCw className={refreshing?'spin':''} size={18}/></button><button className="icon-btn" onClick={()=>setSearchOpen(true)} title="Search"><Search size={19}/></button><button className="icon-btn" onClick={()=>setNotificationsOpen(true)} title="Notifications"><Bell size={19}/></button></div></header>
 {notice&&<div className="notice"><span>{notice}</span><button onClick={()=>setNotice('')}><X size={15}/></button></div>}
 <main>{tab==='now'&&<Now moments={moments} profiles={profiles} loading={loading} onJoin={join} onReact={react} onComment={comment} onCreate={()=>requireUser()&&setCreateOpen(true)} onProfile={setProfileId}/>} {tab==='flow'&&<Feed moments={moments} profiles={profiles} onJoin={join} onReact={react} onComment={comment} onProfile={setProfileId}/>} {tab==='scenes'&&<Scenes moments={moments} profiles={profiles}/>} {tab==='talk'&&<Talk moments={moments} profiles={profiles} user={user} onAuth={()=>setAuthOpen(true)} onComment={comment}/>} {tab==='space'&&<Space user={user} onAuth={()=>setAuthOpen(true)} onLogout={async()=>{await logout();setUser(null);setNotice('Signed out.')}} onSettings={()=>setSettingsOpen(true)} onProfile={setProfileId}/>} {tab==='monetize'&&<Monetize user={user} onAuth={()=>setAuthOpen(true)}/>}</main>
 <button className="spark" onClick={()=>requireUser()&&setCreateOpen(true)}><Sparkles size={20}/><span>SPARK</span></button><nav className="nav">{tabs.map(([id,label,Icon])=><button key={id} className={tab===id?'nav-item active':'nav-item'} onClick={()=>setTab(id)}><Icon size={19}/><span>{label}</span></button>)}</nav>
 {authOpen&&<Auth onClose={()=>setAuthOpen(false)} onDone={async u=>{setUser(u);setAuthOpen(false);await load()}}/>}{searchOpen&&<SearchPanel moments={moments} profiles={profiles} onClose={()=>setSearchOpen(false)} onOpenMoment={id=>{setSearchOpen(false);setTab('now');setTimeout(()=>document.getElementById(`moment-${id}`)?.scrollIntoView({behavior:'smooth',block:'center'}),50)}} onOpenProfile={id=>{setSearchOpen(false);setProfileId(id)}}/>}{notificationsOpen&&<NotificationsPanel user={user} onClose={()=>setNotificationsOpen(false)}/>} {settingsOpen&&<SettingsPanel user={user} onClose={()=>setSettingsOpen(false)}/>} {createOpen&&<CreateMoment user={user} onClose={()=>setCreateOpen(false)} onDone={async()=>{setCreateOpen(false);await load();setNotice('Moment created and live.')}}/>}{profileId&&<PublicProfile profileId={profileId} viewer={user} onClose={()=>setProfileId(null)} onAuth={()=>setAuthOpen(true)} onNotice={setNotice}/>}</div>
}
function Title({eyebrow,title,action}){return <div className="section-title"><div><div className="eyebrow">{eyebrow}</div><h1>{title}</h1></div>{action}</div>}
function Now({moments,profiles,loading,onJoin,onReact,onComment,onCreate,onProfile}){return <div className="page"><section className="hero"><div className="hero-copy"><span className="live-pill"><i/> HAPPENING NOW</span><h1>Be where<br/><em>life is.</em></h1><p>Discover real Moments happening now — around you or anywhere in the WORLD.</p><button className="primary" onClick={()=>document.getElementById('live-layer')?.scrollIntoView({behavior:'smooth'})}><Navigation size={17}/> Explore live</button></div><div className="orbital"><div className="orbit o1"/><div className="orbit o2"/><div className="orbital-core"><Globe2 size={28}/></div></div></section><div id="live-layer"><Title eyebrow="LIVE LAYER" title="Right now"/><div className="moment-list">{loading?<div className="loading">Loading live Moments…</div>:moments.length?moments.map((m,i)=><div key={m.$id}><MomentCard id={`moment-${m.$id}`} m={m} profile={profiles[m.ownerId]} onJoin={onJoin} onReact={onReact} onComment={onComment} onProfile={onProfile}/>{i===1&&<AdBanner320/>}</div>):<EmptyLive onCreate={onCreate}/>}</div></div></div>}
function EmptyLive({onCreate}){return <div className="empty"><Radio size={34}/><h2>No live Moments yet.</h2><p>WORLD shows real activity from real users. Create the first Moment instead of seeing fake activity.</p><button className="primary" onClick={onCreate}><Plus size={17}/> Create Moment</button></div>}
function Feed({moments,profiles,onJoin,onReact,onComment,onProfile}){return <div className="page"><Title eyebrow="LIVE FEED" title="Flow"/><p className="page-intro">Your live activity layer. WORLD does not seed fake users or fake engagement.</p><div className="feed-list">{moments.length?moments.map((m,i)=><div key={m.$id}><MomentCard m={m} profile={profiles[m.ownerId]} onJoin={onJoin} onReact={onReact} onComment={onComment} onProfile={onProfile}/>{i===0&&<AdBanner300/>}</div>):<div className="empty"><Waves size={34}/><h2>Your Flow is waiting.</h2><p>Only real Moments appear here.</p></div>}</div></div>}
function MomentCard({m,profile,onJoin,onReact,onComment,onProfile,id}){const[text,setText]=useState(''),[watch,setWatch]=useState(false),[menu,setMenu]=useState(false);const initials=(profile?.displayName||'W').trim().slice(0,1).toUpperCase();return <article id={id} className="moment"><div className="moment-bg" style={m.imageUrl?{backgroundImage:`url(${m.imageUrl})`}:undefined}/><div className="moment-shade"/><div className="moment-top"><span className="live-pill"><i/> {String(m.category||'NOW').toUpperCase()}</span><span className="age"><Clock3 size={11}/> {timeAgo(m.$createdAt)}</span></div><div className="moment-bottom"><button className="moment-author" onClick={()=>profile?.$id&&onProfile(profile.$id)}><div className="avatar">{initials}</div><span>{profile?.displayName||'WORLD User'} <small>@{profile?.username||'world'}</small></span></button><button className="mini-icon" onClick={()=>setMenu(!menu)}><MoreHorizontal size={16}/></button>{menu&&<MomentMenu moment={m} onClose={()=>setMenu(false)}/>}<h2>{m.title}</h2><p>{m.description||'Live Moment'}</p><div className="card-actions"><button onClick={()=>setWatch(true)}><Play size={14}/> Watch</button><button onClick={()=>onJoin(m.$id)}><Users size={14}/> {m.participantsCount||0} Join</button><button onClick={()=>onReact(m.$id)}><Heart size={14}/> {m.reactionsCount||0}</button><span className="count"><MessageCircle size={14}/> {m.commentsCount||0}</span></div><div className="comment-box"><input value={text} onChange={e=>setText(e.target.value)} placeholder="Say something…" maxLength={500}/><button onClick={()=>{if(text.trim()){onComment(m.$id,text.trim());setText('')}}}><Send size={14}/></button></div></div>{watch&&<MomentWatch moment={m} profile={profile} onClose={()=>setWatch(false)}/>}</article>}
function MomentMenu({moment,onClose}){return <div className="moment-menu"><div><Map size={13}/> {moment.visibility?.toUpperCase()||'NEARBY'}</div><button onClick={onClose}>Close</button></div>}
function MomentWatch({moment,profile,onClose}){const[comments,setComments]=useState([]),[names,setNames]=useState({}),[loading,setLoading]=useState(true);useEffect(()=>{listComments(moment.$id).then(async cs=>{setComments(cs);const ids=[...new Set(cs.map(c=>c.userId).filter(Boolean))],p={};await Promise.all(ids.map(async id=>{try{p[id]=await getUserProfile(id)}catch{}}));setNames(p)}).catch(()=>setComments([])).finally(()=>setLoading(false))},[moment.$id]);return <div className="modal-backdrop" onClick={onClose}><div className="sheet watch-sheet" onClick={e=>e.stopPropagation()}><button className="close" onClick={onClose}><X size={19}/></button><div className="eyebrow">LIVE MOMENT</div><h2>{moment.title}</h2><div className="watch-meta"><span>{profile?.displayName||'WORLD User'}</span><span>·</span><span>{moment.participantsCount||0} joined</span></div>{moment.imageUrl&&<img className="watch-media" src={moment.imageUrl} alt="Moment"/>}<p className="watch-copy">{moment.description||'No description.'}</p><div className="comment-head"><b>Conversation</b><span>{moment.commentsCount||0}</span></div>{loading?<div className="loading compact">Loading…</div>:comments.length?comments.map(c=><div className="comment-row" key={c.$id}><div className="avatar small">{(names[c.userId]?.displayName||'W')[0]}</div><div><b>{names[c.userId]?.displayName||'WORLD User'}</b><p>{c.text}</p><small>{timeAgo(c.createdAt||c.$createdAt)}</small></div></div>):<div className="empty compact"><p>No comments yet.</p></div>}</div></div>}
function SearchPanel({moments,profiles,onClose,onOpenMoment,onOpenProfile}){const[q,setQ]=useState(''),[users,setUsers]=useState([]);useEffect(()=>{let live=true;if(q.trim())listUsers(q).then(x=>live&&setUsers(x)).catch(()=>live&&setUsers([]));else setUsers([]);return()=>{live=false}},[q]);const results=useMemo(()=>moments.filter(m=>`${m.title} ${m.description||''} ${m.category||''}`.toLowerCase().includes(q.toLowerCase())).slice(0,12),[moments,q]);return <div className="modal-backdrop" onClick={onClose}><div className="sheet search-sheet" onClick={e=>e.stopPropagation()}><button className="close" onClick={onClose}><X size={19}/></button><div className="eyebrow">DISCOVER</div><h2>Search WORLD</h2><input autoFocus className="field" value={q} onChange={e=>setQ(e.target.value)} placeholder="Search Moments, people, categories…"/>{q&&results.map(m=><button className="search-result" key={m.$id} onClick={()=>onOpenMoment(m.$id)}><b>{m.title}</b><small>{profiles[m.ownerId]?.displayName||'WORLD User'} · {m.category} · {timeAgo(m.$createdAt)}</small></button>)}{q&&users.map(u=><button className="search-result" key={`u-${u.$id}`} onClick={()=>onOpenProfile(u.$id)}><b>{u.displayName||u.username}</b><small>@{u.username} · {u.followersCount||0} followers · {u.subscribersCount||0} subscribers</small></button>)}{q&&!results.length&&!users.length&&<div className="empty compact"><p>No matching live data.</p></div>}{!q&&<div className="empty compact"><Search size={25}/><p>Search the live WORLD layer.</p></div>}</div></div>}
function NotificationsPanel({user,onClose}){const[items,setItems]=useState([]),[loading,setLoading]=useState(true);useEffect(()=>{if(!user){setLoading(false);return}listNotifications(user.$id).then(setItems).catch(()=>setItems([])).finally(()=>setLoading(false))},[user?.$id]);return <div className="modal-backdrop" onClick={onClose}><div className="sheet" onClick={e=>e.stopPropagation()}><button className="close" onClick={onClose}><X size={19}/></button><div className="eyebrow">ACTIVITY</div><h2>Notifications</h2>{!user?<div className="empty compact"><p>Sign in to see your notifications.</p></div>:loading?<div className="loading compact">Loading…</div>:items.length?items.map((x,i)=><div className="activity-row" key={`${x.$id}-${i}`}><Bell size={15}/><div><b>{x._type==='joined'?'Someone joined your Moment':x._type==='reacted'?'Someone reacted to your Moment':'Someone commented on your Moment'}</b><small>{x.momentTitle||x.text||'New activity'} · {timeAgo(x.createdAt||x.$createdAt)}</small></div></div>):<div className="empty compact"><p>No notifications yet.</p></div>}</div></div>}
function Scenes({moments,profiles}){const groups=categories.map(c=>({category:c,items:moments.filter(m=>m.category===c)})).filter(x=>x.items.length);return <div className="page"><Title eyebrow="REAL ACTIVITY" title="Scenes"/><p className="page-intro">Scenes are built from real live Moments. Nothing is seeded as fake activity.</p>{groups.length?<div className="scene-grid">{groups.map(g=><section className="scene-card" key={g.category}><span>SCENE · {g.items.length} LIVE</span><h2>{g.category.toUpperCase()}</h2><div className="scene-items">{g.items.slice(0,4).map(m=><div key={m.$id}><b>{m.title}</b><small>{profiles[m.ownerId]?.displayName||'WORLD User'} · {timeAgo(m.$createdAt)}</small></div>)}</div></section>)}</div>:<div className="empty"><Map size={34}/><h2>No real Scenes yet.</h2><p>Scenes appear automatically when users create live Moments.</p></div>}</div>}
function Talk({moments,user,onAuth,onComment}){const[text,setText]=useState(''),[selected,setSelected]=useState(moments[0]?.$id||'');useEffect(()=>{if(!selected&&moments[0])setSelected(moments[0].$id)},[moments,selected]);const active=moments.find(m=>m.$id===selected)||moments[0];return <div className="page"><Title eyebrow="LIVE CONVERSATIONS" title="Talk"/><div className="talk-card"><MessageCircle size={22}/><div><b>Talk around real Moments</b><p>Conversations belong to live activity — no fake rooms.</p></div></div>{active?<section className="talk-panel"><select className="field" value={active.$id} onChange={e=>setSelected(e.target.value)}>{moments.map(m=><option key={m.$id} value={m.$id}>{m.title}</option>)}</select><div className="talk-title"><b>{active.title}</b><span>{active.participantsCount||0} joined</span></div><div className="comment-box large"><input value={text} onChange={e=>setText(e.target.value)} placeholder={user?'Join the conversation…':'Sign in to talk…'}/><button onClick={()=>{if(!user)return onAuth();if(text.trim()){onComment(active.$id,text.trim());setText('')}}}><Send size={15}/></button></div></section>:<div className="empty"><MessageCircle size={34}/><h2>No live conversation yet.</h2><p>When real Moments appear, Talk becomes active.</p>{!user&&<button className="secondary" onClick={onAuth}><LogIn size={16}/> Sign in</button>}</div>}</div>}

function Space({user,onAuth,onLogout,onSettings,onProfile}){const[profile,setProfile]=useState(null),[editing,setEditing]=useState(false),[moments,setMoments]=useState([]),[activity,setActivity]=useState([]),[busy,setBusy]=useState(false),[notice,setNotice]=useState('');const load=async()=>{if(!user)return;try{const[p,m,a]=await Promise.all([getUserProfile(user.$id),listUserMoments(user.$id),listActivity(user.$id)]);setProfile(p);setMoments(m);setActivity(a)}catch(e){setNotice(e?.message||'Profile load failed')}};useEffect(()=>{load()},[user?.$id]);if(!user)return <div className="page"><Title eyebrow="YOUR WORLD" title="Space"/><div className="empty"><UserRound size={34}/><h2>Your identity starts here.</h2><p>Sign in to create Moments, follow people, subscribe to creators and manage your WORLD settings.</p><button className="primary" onClick={onAuth}><LogIn size={16}/> Sign in</button></div></div>;const p=profile||{displayName:user.name||'WORLD User',username:user.email?.split('@')[0]||'world',bio:'',followersCount:0,followingCount:0,subscribersCount:0,subscriptionsCount:0,momentsCount:0};return <div className="page"><Title eyebrow="YOUR WORLD" title="Space" action={<button className="secondary" onClick={onSettings}><Settings size={15}/> Settings</button>}/><div className="profile-hero"><div className="profile-avatar large">{(p.displayName||'W')[0].toUpperCase()}</div><div className="profile-main"><h2>{p.displayName}</h2><div className="handle">@{p.username}</div><p>{p.bio||'No bio yet.'}</p><div className="profile-actions"><button className="primary" onClick={()=>setEditing(true)}>Edit profile</button></div><div className="stats"><span><b>{p.followersCount||0}</b> Followers</span><span><b>{p.followingCount||0}</b> Following</span><span><b>{p.subscribersCount||0}</b> Subscribers</span><span><b>{p.subscriptionsCount||0}</b> Subscriptions</span><span><b>{p.momentsCount||moments.length||0}</b> Moments</span></div></div></div>{notice&&<div className="mini-notice">{notice}</div>}<div className="space-tabs"><span className="active">MOMENTS</span><span>JOINED</span><span>SAVED</span><span>ABOUT</span></div><section className="space-grid"><div><div className="subhead"><b>Your Moments</b><span>{moments.length}</span></div>{moments.length?moments.map(m=><div className="mini-moment" key={m.$id}><div><b>{m.title}</b><small>{m.category} · {timeAgo(m.$createdAt)}</small></div><span>{m.status}</span></div>):<div className="empty compact"><p>No Moments created yet.</p></div>}</div><div><div className="subhead"><b>Your activity</b><span>{activity.length}</span></div>{activity.length?activity.map(a=><div className="activity-row" key={a.$id}><span>{a._type==='joins'?<Users size={15}/>:a._type==='reactions'?<Heart size={15}/>:<MessageCircle size={15}/>}</span><div><b>{a._type==='joins'?'Joined a Moment':a._type==='reactions'?'Reacted to a Moment':'Commented on a Moment'}</b><small>{a.text||a.type||'WORLD activity'} · {timeAgo(a.createdAt||a.$createdAt)}</small></div></div>):<div className="empty compact"><p>Your real activity will appear here.</p></div>}</div></section><div className="space-tools"><button className="secondary"><Gamepad2 size={15}/> WORLD PLAY</button><button className="secondary" onClick={onLogout}><LogOut size={15}/> Sign out</button></div>{editing&&<EditProfile profile={p} busy={busy} onClose={()=>setEditing(false)} onSave={async data=>{setBusy(true);try{await updateUserProfile(user.$id,data);setEditing(false);await load();setNotice('Profile updated.')}catch(e){setNotice(e?.message||'Update failed')}finally{setBusy(false)}}}/>}</div>}

function PublicProfile({profileId,viewer,onClose,onAuth,onNotice}){const[p,setP]=useState(null),[loading,setLoading]=useState(true),[following,setFollowing]=useState(false),[subscribed,setSubscribed]=useState(false),[busy,setBusy]=useState(false),[moments,setMoments]=useState([]);useEffect(()=>{(async()=>{try{const[x,m]=await Promise.all([getUserProfile(profileId),listUserMoments(profileId)]);setP(x);setMoments(m);if(viewer&&viewer.$id!==profileId){setFollowing(await isFollowing(viewer.$id,profileId));setSubscribed(await isSubscribed(viewer.$id,profileId))}}catch(e){onNotice(e?.message||'Profile unavailable')}finally{setLoading(false)}})()},[profileId,viewer?.$id]);const act=async kind=>{if(!viewer){onAuth();return}setBusy(true);try{if(kind==='follow'){if(following){await unfollowUser(viewer.$id,profileId);setFollowing(false);setP(x=>({...x,followersCount:Math.max(0,Number(x.followersCount||0)-1)}))}else{await followUser(viewer.$id,profileId);setFollowing(true);setP(x=>({...x,followersCount:Number(x.followersCount||0)+1}))}}else{if(subscribed){await unsubscribeUser(viewer.$id,profileId);setSubscribed(false);setP(x=>({...x,subscribersCount:Math.max(0,Number(x.subscribersCount||0)-1)}))}else{await subscribeUser(viewer.$id,profileId);setSubscribed(true);setP(x=>({...x,subscribersCount:Number(x.subscribersCount||0)+1}))}}}catch(e){onNotice(e?.message||'Action failed')}finally{setBusy(false)}};return <div className="modal-backdrop" onClick={onClose}><div className="sheet public-profile" onClick={e=>e.stopPropagation()}><button className="close" onClick={onClose}><X size={19}/></button>{loading?<div className="loading">Loading profile…</div>:!p?<div className="empty compact"><p>Profile unavailable.</p></div>:<><div className="public-head"><div className="profile-avatar large">{(p.displayName||'W')[0].toUpperCase()}</div><div><div className="eyebrow">WORLD SPACE</div><h2>{p.displayName}</h2><div className="handle">@{p.username}</div><p>{p.bio||'No bio yet.'}</p></div></div>{viewer?.$id!==profileId&&<div className="dual-actions"><button className={following?'secondary':'primary'} disabled={busy} onClick={()=>act('follow')}>{following?<UserCheck size={16}/>:<UserPlus size={16}/>} {following?'Following':'Follow'}</button><button className={subscribed?'secondary':'primary'} disabled={busy} onClick={()=>act('subscribe')}>{subscribed?<CheckCircle2 size={16}/>:<Bell size={16}/>} {subscribed?'Subscribed':'Subscribe'}</button></div>}<div className="stats profile-stats"><span><b>{p.followersCount||0}</b> Followers</span><span><b>{p.followingCount||0}</b> Following</span><span><b>{p.subscribersCount||0}</b> Subscribers</span><span><b>{moments.length}</b> Moments</span></div><div className="profile-note"><Bookmark size={15}/> Follow = social connection. Subscribe = content updates. They are separate.</div><div className="subhead"><b>Moments</b><span>{moments.length}</span></div>{moments.map(m=><div className="mini-moment" key={m.$id}><div><b>{m.title}</b><small>{m.category} · {timeAgo(m.$createdAt)}</small></div></div>)}</>}</div></div>}

function Monetize({user,onAuth}){if(!user)return <div className="page"><Title eyebrow="CREATOR ECONOMY" title="Monetize"/><div className="empty"><CircleDollarSign size={34}/><h2>Build your creator path.</h2><p>Sign in to see your monetization status. WORLD will use real eligibility data — never demo earnings.</p><button className="primary" onClick={onAuth}><LogIn size={16}/> Sign in</button></div></div>;return <div className="page"><Title eyebrow="CREATOR ECONOMY" title="Monetize"/><section className="creator-card"><div className="creator-top"><div><div className="eyebrow">YOUR PATH</div><h2>Creator → Eligible → Approved → Earning → Payout</h2><p>Eligibility is controlled by WORLD policy and can vary by country, content format, originality, trust and advertiser demand.</p></div><CircleDollarSign size={30}/></div><div className="creator-steps"><span>CREATOR</span><span>ELIGIBLE</span><span>APPROVED</span><span>EARNING</span><span>PAYOUT</span></div><div className="creator-foot"><span><CheckCircle2 size={15}/> Original content required</span><span><Shield size={15}/> Safety and fraud review</span><span><Globe2 size={15}/> Country tiers can change</span></div></section><section className="revenue-grid"><div><b>Potential sources</b><p>Moment Ads · Sponsored Moments · Promoted Moments · future Fan Support</p></div><div><b>BE THERE Score</b><p>WORLD can use real participation, watch time, meaningful engagement, originality and trust signals for creator eligibility.</p></div><div><b>No fake balance</b><p>Actual earnings and payout figures should only appear when backed by live platform data.</p></div></section></div>}
function EditProfile({profile,onClose,onSave,busy}){const[displayName,setDisplayName]=useState(profile.displayName||''),[username,setUsername]=useState(profile.username||''),[bio,setBio]=useState(profile.bio||''),[visibility,setVisibility]=useState(profile.visibility||'public'),[error,setError]=useState('');const submit=e=>{e.preventDefault();if(!/^[a-zA-Z0-9_.-]{3,30}$/.test(username))return setError('Username: 3–30 letters, numbers, _ . or -');setError('');onSave({displayName:displayName.trim(),username:username.trim().toLowerCase(),bio:bio.trim(),visibility})};return <div className="modal-backdrop" onClick={onClose}><form className="sheet" onClick={e=>e.stopPropagation()} onSubmit={submit}><button type="button" className="close" onClick={onClose}><X size={19}/></button><div className="eyebrow">PROFILE</div><h2>Edit identity</h2><input className="field" value={displayName} onChange={e=>setDisplayName(e.target.value)} placeholder="Display name" maxLength="100" required/><input className="field" value={username} onChange={e=>setUsername(e.target.value)} placeholder="Username" maxLength="30" required/><textarea className="field area" value={bio} onChange={e=>setBio(e.target.value)} placeholder="Short bio" maxLength="500"/><select className="field" value={visibility} onChange={e=>setVisibility(e.target.value)}><option value="public">PUBLIC</option><option value="friends">FRIENDS</option><option value="private">PRIVATE</option></select>{error&&<div className="error">{error}</div>}<button className="primary wide" disabled={busy}>{busy?'Saving…':'Save profile'}</button></form></div>}
function SettingsPanel({user,onClose}){const key=user?`world-settings-${user.$id}`:'world-settings-guest',[settings,setSettings]=useState(()=>{try{return{...defaultSettings,...JSON.parse(localStorage.getItem(key)||'{}')}}catch{return defaultSettings}}),[section,setSection]=useState('account');const toggle=k=>setSettings(s=>{const n={...s,[k]:!s[k]};localStorage.setItem(key,JSON.stringify(n));return n});const rows=[['Account & Profile','Email, profile and sign-in',UserRound,'account'],['Security & Login','Sessions and account safety',Lock,'security'],['Privacy & Safety','Visibility, location and controls',Shield,'privacy'],['Notifications','Activity alerts',Bell,'notifications'],['Content','Media and sharing preferences',SlidersHorizontal,'content'],['Discovery & Recommendations','Personalized live discovery',Sparkles,'discovery'],['Language & Accessibility','Language, motion and display',Globe2,'language'],['Creator Tools','Creation preferences',Video,'creator'],['Original Content','Originality requirements',CheckCircle2,'original'],['Copyright','Rights and takedowns',Copyright,'copyright'],['Monetization','Eligibility and payout policy',CircleDollarSign,'monetization'],['Blocked & Restricted','People you blocked/restricted',UserCheck,'blocked'],['Reports & Moderation','Safety reports and moderation',Flag,'moderation'],['Community Guidelines','Rules for real activity',FileText,'guidelines'],['Data & Storage','Data saver and media storage',Bookmark,'data'],['App Permissions','Browser/device permissions',Shield,'permissions'],['Activity History','Your recent WORLD activity',Clock3,'history'],['Help & Support','Support and problem reports',MessageCircle,'help'],['Terms & Policies','Legal and policy documents',Copyright,'terms'],['About WORLD','Product information',Globe2,'about']];return <div className="modal-backdrop" onClick={onClose}><div className="sheet settings-sheet" onClick={e=>e.stopPropagation()}><button className="close" onClick={onClose}><X size={19}/></button><div className="eyebrow">WORLD CONTROL</div><h2>Settings</h2><div className="settings-body"><div className="settings-menu">{rows.map(([label,sub,Icon,id])=><button className={section===id?'selected':''} key={id} onClick={()=>setSection(id)}><Icon size={17}/><span><b>{label}</b><small>{sub}</small></span><ChevronRight size={15}/></button>)}</div><div className="settings-content"><SettingsSection section={section} settings={settings} toggle={toggle} user={user}/></div></div></div></div>}
function SettingsSection({section,settings,toggle,user}){if(section==='account')return <div><h3>Account & Profile</h3><p className="muted">{user?.email}</p><div className="setting-note"><Lock size={16}/><span>Email + Password login is enabled. Your WORLD identity is separate from your login email.</span></div></div>;if(section==='security')return <div><h3>Security & Login</h3><div className="setting-note"><Lock size={16}/><span>Keep your password private. Session and account-security controls can be expanded here without changing the profile.</span></div></div>;if(section==='privacy')return <div><h3>Privacy & Safety</h3><SettingToggle label="Private account" desc="Limit who can discover and connect with your profile." value={settings.privateAccount} onClick={()=>toggle('privateAccount')}/><SettingToggle label="Ghost mode" desc="Reduce your public presence in the live layer." value={settings.ghostMode} onClick={()=>toggle('ghostMode')}/><SettingToggle label="Show location" desc="Keep exact location hidden by default; Moment visibility still controls discovery." value={settings.showLocation} onClick={()=>toggle('showLocation')}/><SettingToggle label="Allow messages" desc="Allow people to contact you where messaging is supported." value={settings.allowMessages} onClick={()=>toggle('allowMessages')}/><SettingToggle label="Allow mentions" desc="Control whether people can mention your SPACE." value={settings.allowMentions} onClick={()=>toggle('allowMentions')}/></div>;if(section==='notifications')return <div><h3>Notifications</h3><SettingToggle label="Joins" desc="When someone joins your Moment." value={settings.notifyJoins} onClick={()=>toggle('notifyJoins')}/><SettingToggle label="Reactions" desc="When someone reacts to your Moment." value={settings.notifyReactions} onClick={()=>toggle('notifyReactions')}/><SettingToggle label="Comments" desc="When someone comments on your Moment." value={settings.notifyComments} onClick={()=>toggle('notifyComments')}/></div>;if(section==='content')return <div><h3>Content</h3><SettingToggle label="Autoplay media" desc="Control automatic media playback." value={settings.autoplayMedia} onClick={()=>toggle('autoplayMedia')}/><div className="setting-note"><Video size={16}/><span>Upload only media you own or have permission to use.</span></div></div>;if(section==='discovery')return <div><h3>Discovery & Recommendations</h3><SettingToggle label="Personalized discovery" desc="Use your WORLD activity to tailor the live layer on this device." value={settings.personalizedDiscovery} onClick={()=>toggle('personalizedDiscovery')}/><div className="setting-note"><Navigation size={16}/><span>Nearby, city and WORLD discovery are separate visibility choices on Moments.</span></div></div>;if(section==='original')return <div><h3>Original Content</h3><SettingToggle label="Original-content preference" desc="Keep originality requirements enabled for creator features." value={settings.originalContent} onClick={()=>toggle('originalContent')}/></div>;if(section==='copyright')return <Info title="Copyright" text="Use the report system for content you believe infringes your rights. Keep evidence and ownership records." icon={Copyright}/>;if(section==='monetization')return <Info title="Monetization" text="Eligibility can depend on country, content format, originality, audience quality, trust/safety and advertiser demand. Rules should be controlled by platform policy, not hard-coded demo values." icon={CircleDollarSign}/>;if(section==='moderation')return <Info title="Reports & Moderation" text="Report harmful, abusive, misleading or infringing Moments. Moderation should use real reports and real review outcomes." icon={Flag}/>;if(section==='guidelines')return <Info title="Community Guidelines" text="WORLD is built around real activity. Do not create fake engagement, spam, impersonation, stolen content or misleading Moments." icon={FileText}/>;if(section==='data')return <div><h3>Data & Storage</h3><SettingToggle label="Data saver" desc="Prefer lower data usage for media where supported." value={settings.dataSaver} onClick={()=>toggle('dataSaver')}/></div>;if(section==='about')return <Info title="About WORLD" text="WORLD is a global real-time social discovery platform: see what is happening, be there, participate and create." icon={Globe2}/>;return <Info title={section[0].toUpperCase()+section.slice(1)} text="This WORLD settings category is reserved for its dedicated controls. The structure is ready to expand without putting every option on the first screen." icon={Settings}/>}
function Info({title,text,icon:Icon}){return <div><h3>{title}</h3><div className="setting-note"><Icon size={16}/><span>{text}</span></div></div>}
function SettingToggle({label,desc,value,onClick}){return <button className="setting-toggle" onClick={onClick}><span><b>{label}</b><small>{desc}</small></span><span className={value?'switch on':'switch'}><i/></span></button>}
function Auth({onClose,onDone}){const[mode,setMode]=useState('login'),[name,setName]=useState(''),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);const submit=async e=>{e.preventDefault();setError('');setBusy(true);try{if(mode==='login')await login(email,password);else await register(email,password,name);const u=await getCurrentUser();await onDone(u)}catch(err){setError(err?.message||'Authentication failed')}finally{setBusy(false)}};return <div className="modal-backdrop" onClick={onClose}><form className="sheet" onClick={e=>e.stopPropagation()} onSubmit={submit}><button type="button" className="close" onClick={onClose}><X size={19}/></button><div className="eyebrow">WORLD ACCOUNT</div><h2>{mode==='login'?'Welcome back':'Join WORLD'}</h2><p>Use Email + Password to access the real live layer.</p>{mode==='register'&&<input className="field" value={name} onChange={e=>setName(e.target.value)} placeholder="Name" required/>}<input className="field" type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email" required/><input className="field" type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password" minLength="8" required/>{error&&<div className="error">{error}</div>}<button className="primary wide" disabled={busy}>{busy?'Please wait…':mode==='login'?'Sign in':'Create account'}</button><button type="button" className="switch-link" onClick={()=>setMode(mode==='login'?'register':'login')}>{mode==='login'?'Create a new account':'Already have an account? Sign in'}</button></form></div>}
function CreateMoment({user,onClose,onDone}){const[title,setTitle]=useState(''),[description,setDescription]=useState(''),[category,setCategory]=useState('do'),[visibility,setVisibility]=useState('nearby'),[expires,setExpires]=useState('2'),[file,setFile]=useState(null),[busy,setBusy]=useState(false),[error,setError]=useState('');const submit=async e=>{e.preventDefault();setBusy(true);setError('');try{let imageUrl='';if(file)imageUrl=await uploadMedia(file);await createMoment(user.$id,{title,description,category,visibility,imageUrl,expiresAt:new Date(Date.now()+Number(expires)*3600000).toISOString()});await onDone()}catch(err){setError(err?.message||'Could not create Moment. Check Appwrite permissions and collection IDs.')}finally{setBusy(false)}};return <div className="modal-backdrop" onClick={onClose}><form className="sheet create-sheet" onClick={e=>e.stopPropagation()} onSubmit={submit}><button type="button" className="close" onClick={onClose}><X size={19}/></button><div className="eyebrow">CREATE · SPARK</div><h2>Make a Moment</h2><p>Publish something real that is happening now. It expires automatically.</p><input className="field" value={title} onChange={e=>setTitle(e.target.value)} placeholder="What is happening?" maxLength="200" required/><textarea className="field area" value={description} onChange={e=>setDescription(e.target.value)} placeholder="Describe it…" maxLength="2000"/><div className="create-row"><select className="field" value={category} onChange={e=>setCategory(e.target.value)}>{categories.map(c=><option key={c} value={c}>{c.toUpperCase()}</option>)}</select><select className="field" value={visibility} onChange={e=>setVisibility(e.target.value)}><option value="nearby">NEARBY</option><option value="city">CITY</option><option value="world">WORLD</option><option value="friends">FRIENDS</option><option value="private">PRIVATE</option></select></div><select className="field" value={expires} onChange={e=>setExpires(e.target.value)}><option value="0.5">30 MINUTES</option><option value="2">2 HOURS</option><option value="24">24 HOURS</option></select><label className="upload-field"><ImageIcon size={17}/><span>{file?file.name:'Add image / video / audio'}</span><input type="file" accept="image/*,video/*,audio/*" onChange={e=>setFile(e.target.files?.[0]||null)}/></label>{file&&<div className="media-note"><FileText size={14}/> {file.type||'Media'} · {(file.size/1024/1024).toFixed(1)} MB</div>}{error&&<div className="error">{error}</div>}<button className="primary wide" disabled={busy}><Plus size={17}/>{busy?'Publishing…':'Publish Moment'}</button></form></div>}
function timeAgo(date){if(!date)return'now';const min=Math.max(0,Math.floor((Date.now()-new Date(date).getTime())/60000));if(min<1)return'now';if(min<60)return`${min}m`;const h=Math.floor(min/60);if(h<24)return`${h}h`;return`${Math.floor(h/24)}d`}
